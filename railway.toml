[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m pipeline.run --city Calgary --niche barbershop"
cronSchedule = "0 8 * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
