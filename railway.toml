[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m pipeline.run"
# cronSchedule = "0 8 * * *"   # PAUSED — resume manually once outreach backlog is cleared
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
