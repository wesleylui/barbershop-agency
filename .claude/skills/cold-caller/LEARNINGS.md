# Cold Caller — Learnings

Lessons captured from real cold calls via `/learn` (the `reflect` skill). The
cold-caller skill reads this file first (Step 0) before writing a new script.

Entry format:
```
## YYYY-MM-DD — <subject>
- Task: ...
- Accepted: ...
- Changed/Rejected: ...
- Lesson: ...
```

No entries yet — this file is created empty and fills in after real calls are
reflected on with `/learn`.
